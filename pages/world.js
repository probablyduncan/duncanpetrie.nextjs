// maybe this should be an article?
// we want a map in the image container
// on the right are lists with links to world cards
// when you hover over a link it circles the location on the map

// get static props, all world articles
// pick some at random to display, plus maybe some good starters?

export default function World() {
    return <div>world world world</div>
}